services:
  # --- BACKEND SERVICE ---
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app:z
    command: uvicorn src.api.server:app --host 0.0.0.0 --port 8000 --reload
